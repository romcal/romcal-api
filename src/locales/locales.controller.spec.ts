import { LocalesController } from './locales.controller';

describe('Locales Controller', () => {
  const controller: LocalesController = new LocalesController();

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
